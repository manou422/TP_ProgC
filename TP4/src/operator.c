#include <stdio.h>
#include <string.h>

int main() {
int a = 16;
int b = 3;
int res = 0
res = a+b;
printf("L'addition = %d", res);

res = a-b;
printf("La soustraction = %d", res);

res = a*b;
printf("La multiplication = %d", res);

res = a/b;
printf("La division = %d", res);

res = a%b;
printf("L'addition = %d", res);

bool egal = (a==b);
printf("a est égal à b ? %s\n", egal ? "true" : "false");

bool superieur = (a>b)
printf("a est égal à b ? %s\n", superieur ? "true" : "false");

}