#include <stdio.h>
#include <string.h>
#include <math.h>

int main() {
    int num1 = 10;
	int num2 = 5;
	string op = '+'

	switch (op) {
		case + : return num1 + num2;
		case - : return num1 - num2;
		case * : return num1 * num2;
		case / : return num1 / num2;
		case % : return num1 % num2;
		case & : return num1 & num2;
		case | : return num1 | num2;
		case ~ : return num1 ~ num2;
	}

}